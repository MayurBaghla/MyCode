package Collections;

public class HashTable {

	public static void main(String[] args) {
//similar to hashmap but it is synchronized
		//stores value in key and value pair
		//key is specified as an object..every object has a hashcode and on that hashcode..
		//
		
		
		HashTable ht = new HashTable();
		
		//ht.put("A","Mayur");
	}

}
